# Worker B — local transfer and recovery workflows

Snapshot manifest SHA-256: `855dce0aea7b60537f2a087e9a9e108a4941e1c337dd47bc04ad666ed21c732c`. Worker copy only: `/Users/aligeek/Documents/harbor-db-parallel-20260918/worker-b`. Original roadmap and original BACKLOG/PROGRESS/PARALLEL/UX_FLOWS read. Coordinator owns acceptance and integration. No Git operations or release/publication performed.

## Scope reconciliation

- ADV01: existing full CSV/typed JSONL export, bounded native streams, partial files, explicit UI review/cancel, filesystem fault and memory evidence. Preserve and rerun relevant regression gate.
- ADV02: existing SQL CSV/JSONL parsing, native file grants, explicit mapping/encoding/NULL/error policy, independent batches, cancellation and uncertainty reporting. Mongo EJSON remains worker C's model-specific scope by coordinator assignment.
- ADV09: backend/strict IPC existed; renderer and tests absent. Added explicit destination selection/catalog loading, bounded preview, position-based duplicate source-column mapping, mandatory rerun/batch consent, typed target, scoped rows/batches, progress/cancel. Opening is inert. Added actual 3x3 PostgreSQL/SQLite/DuckDB matrix and failure tests.
- ADV12: native service/tool/UI existed; restore drill unfinished. Fixed actual restored-database catalog verification by opening a separate read-only SqlService profile bound to the new database, cancelling and closing it independently. Original session target checks remain intact.

## Current matrix and limits

Transfer sources and destinations: PostgreSQL, SQLite, DuckDB in all nine directions, with different profile identities. Same local file is rejected; destination is an existing base table. One read-only source snapshot and independent transactional destination batches; no cross-database transaction, schema creation, upsert, retry, resume, identity override or implicit key remapping. Limits: two running transfers; 20 preview rows / 256 KiB clipped display, 200 columns, 500 rows / 8 MiB per batch, explicit maximum up to 1,000,000 rows (default 10,000). Actual fields limited to 1,000,000 characters. Generated/identity-always fields omitted; DuckDB generated-column destinations unsupported. Native temporal destination types are disabled and rejected: use TEXT to preserve exact source representation rather than imply equivalent timezone/precision behavior. Floating-point narrowing is now rejected before writing; exact decimal precision guards retained.

Backup: selected pinned native PostgreSQL custom archives only; actual server/tool pair 18.6/18.6 on this macOS ARM64 host. New unique non-production restore databases only, no overwrite/drop/retry. Direct connections only; SSH/client-certificate native workflows unsupported. Global roles/ownership/ACLs, physical backup, PITR and disaster recovery excluded. Catalog completion is explicitly distinguished from representative-content verification. Other platforms/signing/packaged final-candidate checks remain coordinator gates.

## Verification (2026-09-18, transfer/backup checkpoint)

Command prefix for each label: `rtk proxy python3 /Users/aligeek/Documents/harbor-db-parallel-20260918/run.py worker-b LABEL`.
Logs: `/var/folders/tx/rf_fyjp13vqgywb46ydkh9_80000gn/T/harbor-onboarding-20260918-y6pr0dek/logs/parallel-worker-b-LABEL.{log,json}`. Runner JSON's cwd field is historically hardcoded to original checkout; actual subprocess cwd is worker-b, confirmed by Vitest/Vite output and run.py source.

| Label | Arguments after prefix | Result |
| --- | --- | --- |
| backup-baseline | `HARBOR_NATIVE_BACKUP=1 npm test -- tests/native-backup.integration.test.ts` | exit1;4 failures because test temporary directory used macOS /var symlink; canonical fixture path corrected without relaxing output checks |
| backup-drill | same | exit1;3pass/1fail; actual restore succeeded, catalog query incorrectly reused source profile and was rejected |
| backup-isolated | same | exit0;4/4pass; exact bigint/decimal/binary/enum, views/index/FK/function/identity sequence; new archive mode0600; existing output/database rejection; changed archive; real lock-blocked dump cancellation; native restore failure retains new DB with zero public tables after native transaction rollback |
| transfer-matrix | `HARBOR_TRANSFER=1 npm test -- tests/database-transfer.integration.test.ts` | exit1;13fail/3pass; fixture DELETE lacked required typed confirmation. Initial cleanup also rejected; five uniquely named leftovers and their function subsequently removed in cleanup-confirmed |
| transfer-matrix2 | same | exit0;16/16pass;9 source/target pairs,3 destination rollback cases, bounded preview/505-row cap, source write rejection, identity/generated/schema drift, real trigger cancellation, decimal narrowing |
| types-first | `npm run typecheck` | exit2;new renderer used dataType instead of existing type; corrected |
| unit-types | `npm run typecheck` | exit2;new matrix service union connect needed explicit empty secrets; corrected |
| transfer-unit | `npm test -- tests/database-transfer.test.ts tests/import-jobs.test.ts tests/import-parser.test.ts` | exit1;13 existing pass/8 new fail due test profiles missing required host/port; corrected |
| transfer-unit2 | `npm test -- tests/database-transfer.test.ts` | exit0;8/8pass including single-use tokens, in-place profile drift, scoped-preview disconnect, uncertain writes/no retry, source-loss unwritten rows |
| native-tools-unit | `npm test -- tests/native-backup-tools.test.ts tests/import-number-fidelity.test.ts` | exit0;9/9pass; real bounded child-process/file behavior plus numeric narrowing guard unit checks |
| build-ui | `npm run build` | exit0;typecheck/main/preload/renderer build passed36.11s |

## Ownership and remaining work

Coordinator granted narrow QueryEditor lease: only DatabaseTransfer import and adjacent JSX with same FullExport props and key by profile/database. No other shared files changed. NativeBackup Diagnostics composition already existed.

Implemented: transfer component; profile/generation review race protection; terminal buffered/unwritten accounting; isolated native restore verifier; numeric precision guard; per-batch native schema drift detection. Backend, native Electron UI, and bounded >384MiB transfer benchmark completed below. No full-ticket or cross-platform acceptance claimed; coordinator owns final integrated acceptance. All new fixture resources are synthetic, uniquely named, loopback or disposable temporary files.


## Additional verified safety work

- `transfer-matrix-faults`: `HARBOR_TRANSFER=1 npm test -- tests/database-transfer.integration.test.ts`, exit0,18/18. Real TCP-proxy lost COMMIT acknowledgement: exactly one observed COMMIT,2 actual persisted rows classified uncertain, no replay. Real PG/DuckDB numeric-to-REAL/DOUBLE narrowing rejected.
- `backup-expanded`: `HARBOR_NATIVE_BACKUP=1 npm test -- tests/native-backup.integration.test.ts`, exit0,7/7. Adds real cancellation during restore CREATE INDEX, retained target/actual rolled-back transaction contents/native-session cleanup; profile/disconnect invalidation; version-policy fault injection explicitly separate from native18.6 compatibility.
- `transfers-regression`: `HARBOR_INTEGRATION=1 npm test -- tests/streaming.integration.test.ts tests/streaming-files.test.ts tests/streaming-import.integration.test.ts tests/local-streaming-import.test.ts tests/import-parser.test.ts tests/import-jobs.test.ts tests/database-transfer.test.ts tests/import-number-fidelity.test.ts tests/native-backup-tools.test.ts`, exit0,81/81.
- `inspect-cleanup` and `cleanup-confirmed`: isolated Node scripts in work/ inspected and dropped only first failed-run tables with suffix ce7927e262c9 and its delay function. Exit0; final matching catalog list empty. Scratch scripts are not integration artifacts.
- Coordinator granted additional narrow lease in SqlService.openImport only. Per-batch native column fingerprint executes on the dedicated writer session after BEGIN and existing metadata lock; mismatched type/null/default/generated/identity metadata stops the batch. Local writers check native metadata inside BEGIN IMMEDIATE / DuckDB transaction. SQLite schema-version changes conservatively invalidate review, including unrelated DDL; DuckDB snapshot/commit conflict semantics remain native.
- `schema-drift`: real PG/Maria/SQLite/Duck schema-drift4/4 passed; combined local import suite had1 existing timing-dependent cancellation failure. Cancellation test now aborts after an observed actual native INSERT returns, before the next operation, and verifies actual transaction rollback.
- `schema-drift2`: `HARBOR_INTEGRATION=1 npm test -- tests/import-schema-drift.test.ts tests/local-streaming-import.test.ts tests/streaming-import.integration.test.ts tests/database-transfer.integration.test.ts`, exit0,30pass/18skip (transfer flag absent; skipped cases not claimed).
- `transfer-final-backend`: `HARBOR_TRANSFER=1 HARBOR_NATIVE_BACKUP=1 HARBOR_INTEGRATION=1 npm test -- tests/database-transfer.integration.test.ts tests/native-backup.integration.test.ts tests/import-schema-drift.test.ts tests/database-transfer.test.ts tests/native-backup-tools.test.ts tests/import-number-fidelity.test.ts tests/streaming-import.integration.test.ts tests/local-streaming-import.test.ts tests/streaming.integration.test.ts tests/streaming-files.test.ts tests/import-parser.test.ts tests/import-jobs.test.ts`, exit0,112/112 across12 files,10.99s. Adds real SQLite generated-column omission, Duck generated-table rejection, and same-inode hardlink rejection before opening a target session.
- `build-final-transfers`: `npm run build`, exit0,15.66s; final runtime candidate typecheck/main/preload/renderer passed. `lint-final-transfers`: explicit16 owned files, exit0. Only UI test-source refinements since that runtime build.

Current changed existing files: src/shared/database-transfer.ts; src/main/persistence/{database-transfer,import-writer,local-import-writer,native-backup}.ts; src/main/engines/sql.ts (leased openImport hunks only); src/renderer/src/components/QueryEditor.tsx (leased import+JSX only); tests/local-streaming-import.test.ts; tests/native-backup.integration.test.ts.
New files: src/renderer/src/components/DatabaseTransfer.tsx; tests/database-transfer{,.integration,-benchmark}.test.ts; tests/database-transfer.e2e.ts; tests/native-backup{.e2e,-tools.test}.ts; tests/import-number-fidelity.test.ts; tests/import-schema-drift.test.ts; this ledger.

## Completed transfer/backup checkpoint

- `ui-tests-types`: `npm run typecheck`, exit0,5.01s.
- `transfer-native`: `HARBOR_INTEGRATION=1 HARBOR_NATIVE_BACKUP=1 npm exec -- playwright test tests/database-transfer.e2e.ts tests/native-backup.e2e.ts tests/import-wizard.e2e.ts tests/local-analytics-export.e2e.ts --trace=off`, exit0,5/5 native Electron tests,24.6s. Actual PG→SQLite mapping/251 rows/3 batches/later rollback/cancel, SQL import, DuckDB file preview, full PG export, native18.6 backup/new unique restore with representative exact-value content verification. Opening and preview paths verified inert. UI lease released.
- `transfer-memory`: `HARBOR_TRANSFER_BENCHMARK=1 npm test -- tests/database-transfer-benchmark.test.ts`, exit0,1/1,11.40s. Vitest intercepted the console metric output, so one follow-up with `--disableConsoleIntercept` was required to capture actual measurements.
- `transfer-memory-metrics`: same plus `--disableConsoleIntercept`, exit0,1/1,11.53s. Actual source payload409,888,894 bytes;25,000 committed rows/250 batches;10,823ms job duration;heap delta60,150,440 bytes (<128MiB);RSS delta192,069,632 bytes (<192MiB);max buffered100 rows;max concurrent onRow1. Destination independently measured25,000 rows/409,600,000 payload characters. This is one macOS host/process measurement, not a universal memory bound. Benchmark tables dropped in finally. Resource lease released.
- `native-cleanup-check`: read-only catalog/session inspection, exit0;no remaining uniquely named backup fixture databases or native backup sessions.

Runtime candidate has not changed since the112-test/build checkpoint; later changes refine test sources and this ledger. Integration inventory/hashes live outside durable documentation in `work/worker-b-checkpoint.json`; shared sql.ts and QueryEditor.tsx require only the explicitly leased patches in work/, not whole-file replacement. Remaining ticket/platform gates include final coordinator integration, packaged multi-OS/native dependencies/signing, documented unsupported transfer types/topologies, and the worker C Mongo file workflow.

## Next assigned package

EXT-08 managed PG/MySQL/SQL Server, EXT-09 CockroachDB/YugabyteDB, EXT-10 TiDB/Vitess, DB-11 Redshift: reconcile transport and distinct target identity/capabilities/serialization, implement isolated modules, propose shared contracts/registry dependencies to coordinator before edits, and exercise feasible disposable targets. Managed/cloud acceptance requires separately authorized actual targets; no credentials or account provisioning inferred.

## Managed and compatible-product implementation in progress

Coordinator granted worker-copy leases for5 engine names, managed profile field, Redshift deployment/credential-expiry field, capability definitions, narrow new-session validators in SqlService/MssqlService, minimal main/IPC/form/query wiring with exact integration hunks, and managed IPC target validation/database filtering. Root retains shared-file integration ownership. No dependency additions.

New modules: shared managed-deployment.ts and compatible-sql.ts; main CompatibleSqlService; ManagedDeploymentFields and CompatibleSqlFields/Context; focused policy, native-driver, and desktop tests. Explicit product identities: CockroachDB, YugabyteDB YSQL, TiDB, Vitess, Redshift. Basic catalogs use information_schema for the first4 and Redshift SVV views separately. Constraint/index reconstruction, reviewed grid edits/import/admin/backups are explicitly unavailable for these products; empty structure lists mean not inspected. Initial Vitess and Redshift scopes require guarded browsing. Supported transaction controls for CockroachDB/YSQL/TiDB never replay statements or transactions. Exact PG textual values and pinned MariaDB lossless options retain numeric/temporal/JSON/binary data. Display cap8MiB; full export uses consumer backpressure and an8MiB row ceiling. Cancellation closes the physical session and reports that server rollback is unconfirmed.

Managed presets retain original PG/MySQL/MSSQL product/version gates. They require verified TLS, explicit database, direct endpoint (Supabase session pooler optional), and explicit reconnect. Password/already-issued temporary DB credential modes are distinct from unsupported IAM/Entra token acquisition. Temporary credential expiry is validated as ISO8601 UTC and checked before every new physical session, while already-authenticated sessions remain usable. IPC rejects other requested database/schema contexts and filters database listings. This is not SQL authorization: database roles remain authoritative. Cloud SQL proxy and transaction pooling are unavailable. Managed/cloud/Redshift runtime acceptance requires actual authorized targets; local policy fixtures are not cloud evidence.

Primary preflight references, reviewed2026-09-18:

- [CockroachDB licensing](https://www.cockroachlabs.com/docs/stable/licensing-faqs): start-single-node development requires no license key; no multi-node license or deployment claim. [Transaction behavior](https://www.cockroachlabs.com/docs/stable/developer-basics.html): client restart errors remain explicit.
- [Yugabyte Docker quickstart](https://docs.yugabyte.com/stable/quick-start/docker/): local single-host fixtures are development scope. Official2026.1.1.2-b9 provides ARM64 image.
- [TiDB compatibility](https://docs.pingcap.com/tidb/stable/mysql-compatibility/): protocol reuse does not supply MySQL stored routines/triggers, replication, plans or DDL behavior. Local unistore development backend cannot verify distributed TiKV behavior.
- [Vitess compatibility](https://vitess.io/docs/24.0/reference/compatibility/mysql-compatibility/): separate routing/transaction restrictions; initial guarded one-keyspace scope excludes shard/tablet overrides.
- [Redshift differences](https://docs.aws.amazon.com/redshift/latest/dg/c_redshift-and-postgres-sql.html), [SVV_TABLES](https://docs.aws.amazon.com/redshift/latest/dg/r_SVV_TABLES.html), [SVV_COLUMNS](https://docs.aws.amazon.com/redshift/latest/dg/r_SVV_COLUMNS.html): independent warehouse catalogs and capabilities, bound to current_database; no PostgreSQL administration inheritance. [Cursor resource implications](https://docs.aws.amazon.com/redshift/latest/dg/declare.html): cursor materialization can consume leader-node memory/disk; query/export review retains cost context and never provisions compute.
- [Supabase modes](https://supabase.com/docs/guides/database/connecting-to-postgres) and [Neon pooling](https://neon.com/docs/connect/connection-pooling): Harbor relies on persistent tab sessions; transaction pooling is rejected, known pooler endpoint mistakes detected. [RDS IAM expiry](https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/UsingWithRDS.IAMDBAuth.Connecting.html) informs explicit credential lifetime handling, not a claim of implemented AWS identity login.

### Current checks and resources

- `compatible-types`: exit2; PG Query declaration needed QueryArrayConfig and newly added engines needed renderer maps. Corrected. `compatible-types2`/`compatible-types3`: typecheck exit0.
- `managed-compatible-policy`: exit0,7/7; updated managed IPC target tests `compatible-policy-final`:exit0,8/8.
- `compatible-build`: exit2 because a test-only generic catalog target type omitted optional connectionId; corrected. `compatible-build2`: exit0,15.88s final main/preload/renderer/typecheck for current native UI run.
- `cockroach-native`: exit1 fixture not ready,6 skipped. Image initialization shell rejected listen_addr=0.0.0.0 before server startup. Inspected its script, replaced only this owned stopped container with native binary entrypoint. CLI readiness command initially combined SET CLUSTER SETTING and SELECT, which Cockroach rejected as a multi-statement transaction; split commands succeeded.
- `cockroach-native2`: exit0,6/6 actual Cockroach25.4.0. `cockroach-safety`: exit0,16/16 across4files (8 actual Cockroach scenarios,1 local MySQL TLS policy fixture,7 policy cases). Actual serializable read/write conflict left failed transaction unreplayed; TCP proxy dropped real COMMIT response, exactly one COMMIT and one persisted row, explicit uncertainty reported.1201 rows streamed with max active sink1; display7-row truncation; abort and sink failures; exact values/parameters; table filters/catalog; raw session/script guards; cancellation/timeout.
- Local MySQL TLS policy test verifies trusted fixture CA, wrong trust rejection, disabled-verification rejection, future-clock credential expiry blocks fresh sessions but preserves existing authenticated session. This is explicitly not Cloud SQL runtime acceptance.
- Cockroach fixture `harbor-b-cockroach-20260918` was removed after matching catalog cleanup proof. Native ARM64v25.4.0 image digest6d4182e984256f8506301912ea26039a95cbc294271d8d9ef42e19c03ffdcb21; loopback26258;1.25GiB/2CPU;in-memory store640MiB (product minimum, replacing proposed256MiB),cache64MiB,SQL128MiB;no host data mount or restart. Diagnostics reporting disabled. Repro command inventory in work/cockroach-fixture.json.
- `cockroach-ui`: exit1, test expected nonexistent Cancel button after all product assertions; Close locator corrected. `cockroach-ui2`: exit0,1/1 native Electron. `compatible-lint`: unnecessary regex escape rejected; corrected, `compatible-lint2` exit0 on10 owned files. Prettier confined to those owned files.
- TiDB8.5.5 ARM64 digestabf0d4267617ced2ed335d2ae0f6e8f59869252daef682a762cc8c503493b046 ran sequentially after Cockroach removal at localhost14000,1.5GiB/2CPU. Startup attempts rejected unknown --port flag, then256MiB tmpfs filled due unistore preallocation. Corrected to documented -P=4000 and disposable writable container layer; `tidb-readiness3` exit0 with actual version8.0.11-TiDB-v8.5.5. No host data mount; telemetry false. Unistore is development scope, not distributed TiKV evidence.
- `tidb-native`: exit1,5fail/1pass/2skip. Native evidence exposed LIMIT/OFFSET string binding incompatibility, unsupported SET TRANSACTION READ ONLY, and SLEEP timeout returning1 after server deadline. Safe integer parameters are now bound as numbers while larger integers/decimals stay strings; unavailable TiDB READ ONLY is explicit in product/export UI; client deadline precedes server max_execution_time by500ms. Server grants and SQL guard remain boundary; no noop-readonly workaround.
- `tidb-native2`: exit0,6pass/2skip. Added temporal/JSON precision, implicit DDL commit and actual lost COMMIT acknowledgement to both protocols. `tidb-safety`: exit1,7pass/1fail/2skip because DDL test left its deliberately committed fixture row, making expected stream count1202; fixture cleanup corrected. `tidb-safety2`: exit0,9pass/1 intentional CRDB-only skip. Actual uncertain COMMIT persisted exactly once; DDL implicitly committed prior transaction, surfaced idle state, never implied rollback undid it.
- `compatible-types4`: exit0,5.21s. `compatible-build3`: exit0,19.33s. `tidb-ui`: exit1, unexpected plural saved connection name; cause unproven. Added explicit form-value assertion before saving without changing runtime. `tidb-ui2`: exit0,1/1,4.5s, real Electron exact numeric values/full JSONL export/managed fields/native MySQL TLS IPC target denial. Electron teardown complete, slot released to C.
- `tidb-cleanup`: exit0, zero remaining compat tables. Owned TiDB container removed. Coordinator granted sequential3GiB/2CPU Yugabyte then Vitess, without overlap. Yugabyte2026.1.1.2-b9 ARM64 image fdccfe65f854b23781facae04d65bcdb51f0797297dfd3aa47ceff81682e3663 now runs as harbor-b-yugabyte-20260918 at localhost15435; no host mount, restart=no, master512MiB/tserver1GiB memory trackers inside3GiB total, max40 SQL connections, connection manager disabled. UI and callhome disabled using documented [yugabyted flags](https://docs.yugabyte.com/stable/reference/configuration/yugabyted/). Image declares two anonymous data volumes; remove only its own volumes with container cleanup.
- `yugabyte-native`: exit0,16pass/2skip across native+policy3files (8 actual YSQL cases,8 policy; CRDB conflict and TiDB DDL are intentional skips). Image tag2026.1.1.2-b9 reports actual SQL version15.12-YB-2026.1.1.2-b0. Raw CLI initially tried internal127.0.0.1 and failed because yugabyted bound container IP; observed172.17.0.5 succeeded. Host loopback15435 native tests worked. Actual memory510MiB; no OOM.
- `yugabyte-transport`: exit0,2/2 real pinned SSH and TLS-gateway queries. TLS gateway implements only PG SSLRequest/TLS, forwards unchanged database traffic to actual YSQL; trusted certificate and hostname verified, untrusted CA and wrong original hostname across SSH rejected. This is not native YSQL-server TLS configuration or cloud acceptance.
- `compatible-build4`:exit0,21.61s; `compatible-types5`:exit0,7.45s. Added rejection of managed presets on compatible-product profiles. `yugabyte-ui`:exit0,2/2 native Electron,7.3s; YSQL form/query/exact-values/export plus offline Redshift persisted serverless/already-issued temporary-expiry metadata, cost/IAM-unavailable context, disconnected execution disabled, empty history. Redshift network was never connected. YSQL fixture catalog empty, owned container and its2anonymous volumes removed; Electron released to root.
- Vitess24.0.2 vttestserver/mysql8.4.6-Vitess AMD64-only image401dd940b7cd8fa0b9d71a630dd586dce30e6f4c3d44775f57c237579c59a021 started under emulation after YSQL removal,3GiB/2CPU/1024pids/no restart, no host mounts. Loopback15306 maps only VTGate33577. One unsharded keyspace harbor, max80 MySQL connections; online DDL disabled. Actual memory556MiB. `vitess-readiness`:exit0, native version/comment confirm actual Vitess24.0.2. This does not establish native ARM Vitess, sharding, replication or production topology acceptance.
- `vitess-native`:exit1,6pass/1fail/5skip and3 unhandled rejections. VTGate returns physical vt_harbor_0 schema from information_schema after rewriting the bound keyspace predicate; mapping now retains logical configured keyspace for browsing/query routing. Pinned MariaDB3.5.4 destroy() opens another authenticated connection and issues KILL, rejected by VTGate. CompatibleSqlService now uses the driver's documented custom-stream hook, owns original socket, and closes that socket directly, including pending requests; no hidden reauthentication/KILL. Existing SqlService behavior was reported to coordinator, outside this package's narrow validator lease.
- `vitess-native2`:exit0,7pass/5skip and no unhandled errors;6 actual VTGate adapter cases+1 real SSH case. Added `vitess-cancel-proof`:exit0,2pass/1skip; actual SSH forwarded-channel count is exactly2 (metadata+tab), cancellation creates no additional authenticated connection. Client cancellation still does not claim confirmed server rollback.
- `compatible-format-final`:exit0,only11 owned new source/test files. `compatible-build-final`:exit0,19.85s main/preload/renderer/typecheck. `compatible-lint-final`:exit0,11 owned files. `compatible-regression`:exit0,84pass/11skip across11files,3.94s; current VTGate/SSH/native local managed-MySQL-TLS and policy checks plus existing persistence/capabilities/adapter/SQL/table-query/connection-experience regressions. Intentional product/flag skips remain unverified, not passes. Shared-file baselines reconstructed in work/ and verified exactly against baseline.json; SqlService/QueryEditor reconstruct against already-integrated first checkpoint hashes, so the next leased patches exclude old transfer hunks.
- `vitess-transport-final`:exit0,3pass/2skip. Adds real metadata TCP loss, observed failed status, disconnected tab state, rejected new-tab query with zero additional forwarded channels until explicit reconnect. Runtime now refuses operations while product connection state is not connected. `compatible-build-lifecycle`:exit0,18.01s; focused lint exit0. These checks preceded the final TLS close fix below.
- Recreated native TiDB8.5.5 with self-signed fixture certificate/SAN127.0.0.1 and private key0600, both copied only into owned disposable container, using documented [TiDB server TLS configuration](https://docs.pingcap.com/tidb/stable/enable-tls-between-clients-and-servers/). Same1.5GiB cap, no concurrent Vitess. `tidb-tls-readiness`:exit0. `tidb-final-tls`:exit1 although13 assertions passed, due8 actual uncaught TLS errors. Root cause: destroy(Error) on underlying TCP socket can re-emit into the TLS wrapper after the pinned driver's fatalError removes wrapper listeners. Corrected by signaling EOF on the owned original stream before destroying without Error; connector watches end rather than close, rejects pending operations and closes TLS itself. No global handler suppresses errors. Existing KILL helper remains unused.
- `tidb-final-tls2`:exit0,13pass/2 intentional skips (CRDB conflict, PG TLS gateway), zero unhandled errors. Actual native TiDB TLS/trust failure/original hostname across SSH; cancellation without a hidden authenticated session; metadata-loss explicit reconnect; all current TiDB native transaction/DDL/lost-ack/exactness/stream regressions. `tidb-auth`:exit0,1pass/10 filtered skips; native password failure is redacted, unique user SELECT grant works, and native server rejects INSERT even with Harbor guarded browsing disabled. Fixture user dropped. `tidb-tls-cleanup`:exit0, no compat tables or unique native-auth users; owned TLS container removed with no OOM. Vitess fresh disposable fixture restored for pending desktop run, no overlap. `compatible-lint-transport`:exit0 on the3 changed files.

### Driver and remaining acceptance boundaries

Final package checkpoint: `compatible-build-transport` exit0,15.84s (includes typecheck); `compatible-lint-transport` exit0 for changed files after earlier11-file lint; `compatible-final-regression` exit0,85pass/13 explicit product/flag skips across11files,3.51s. `vitess-ui` exit0,1/1 native Electron4.2s on this final runtime candidate; exact BIGINT/DECIMAL duplicate output names, correct one-keyspace context, full JSONL export and managed preset fields. App teardown completed, Electron slot released to C. `vitess-cleanup-final` exit0,empty compat catalog, owned container and anonymous volume removed without OOM. All B compatible-product containers are now removed; generated TiDB private key deleted, public CA/config and safe reproduction scripts remain in ignored work/. Images remain cached; other workers' resources were not modified. Frozen owned source/tests/ledger plus11 exact shared-file patches are in work/compatibility-checkpoint; subsequent Db2 work must not alter that snapshot.

No package or lockfile changes. Existing pinned packages inspected: pg8.23.0 MIT (Node>=16), pg-cursor2.22.0 MIT, mariadb3.5.4 LGPL-2.1-or-later (Node>=20), tedious20.0.0 MIT (Node>=22). These use existing distribution paths; current execution uses isolated Node24.19/npm11.19 and native Electron44.3 on macOS ARM64. Existing licenses remain applicable. Final integrated packaged/other-OS checks belong to coordinator; these source/dev desktop checks are not packaging or signing acceptance.

Local topology scope: Cockroach single insecure node, Yugabyte single insecure YSQL node, TiDB unistore with separately exercised native TLS/password/restricted grants, Vitess one unsharded keyspace and no configured native auth provider under AMD64 emulation. Cockroach/YSQL native server TLS/password auth, production failover/restart/topology, Vitess native auth/TLS and multi-shard behavior remain unverified. YSQL gateway-TLS evidence is narrower and identified above. Managed cloud presets and actual Redshift provisioned/serverless execution require authorized endpoints, restricted credentials and a cost boundary; no cloud target was attempted. Redshift cursor behavior remains runtime-unverified. Constraint/index reconstruction and grid mutations/import/admin/backup remain deliberately unavailable for compatible products, not inherited from base protocols.


## 2026-09-18 — DB-29 IBM Db2 independent implementation checkpoint

**Status:** source implemented with explicit runtime prerequisites; full ticket NOT accepted. Coordinator assigned DB29 and granted narrow profile/contracts/IPC/main/form/capabilities/manifest plus SQL dialect, QueryEditor context, transfer consistency, worker-entry and native-unpack leases. Compatibility/Redshift frozen checkpoint remains unchanged (`e72ff125679fe61654067e2fc56940cbb38c5f87f5525dc138c15c22b175c955`). Root owns combined integration and acceptance.

Implemented a separate `db2` identity and guarded LUW11.5/12.1 UTF-8-only catalog/query/table/export adapter. `ibm_db@4.0.1` is an exact OPTIONAL PEER, not an automatically installed proprietary driver. `npm install --package-lock-only --ignore-scripts --no-audit --no-fund` changed only the root lockfile package entry; `node_modules/ibm_db` remains absent. Node tarball source inspected read-only, SHA256 `54deb4dad413844e9104f5c099bf3b4fb8a00d40e906da7f275558c1da189774`. No CLI or native addon executed. Installer default is floating, includes proprietary CLI terms, and platform pin logic differs for MacARM12.x versus MacIntel11.5.x. Actionable artifact, license, target OS/architecture, addon/CLI resources, native test fixture and packaging prerequisites are documented in `docs/db2-runtime.md`.

Native source inspection found unsafe generic DECIMAL/NUMERIC/DECFLOAT→double and TIMESTAMP→Date conversions, plus NUL-terminated native text creation. Generic unsafe outputs are refused from metadata before fetch. Catalog/table reads use trusted bounded HEX projections; fatal UTF-8 decoding preserves Unicode/NUL, binary remains bytes, decimal/microsecond-or-nanosecond timestamp representations avoid JS numeric/Date conversion. Table text/binary limited to511 declared bytes, max128 columns; unknown/LOB/XML/oversize types refused. Raw exact INTEGER/BIGINT/IEEE/date/time/bit and bounded VARBINARY are checked; raw text/decimal/timestamp require explicitly chosen bounded binary casts. Catalog max1000objects; primary-key order inspected, defaults/indexes/FKs/fullDDL explicitly uninspected. Bound structured equality/null/order filters and server OFFSET/FETCH; contains filter unavailable. Writes/transactions/grid edits/import/admin/backup unsupported.

All native calls isolated in a disposable child process per physical session, four query sessions plus metadata per profile. Product/version/database encoding checked per connection. One acknowledged streamed row at a time; display8MiB/row1MiB bounds; native metadata prevents unbounded LOB fetching. Parent query/connect deadlines kill even synchronously blocked child; native query timeout also set. JavaScript heap128MiB is NOT a native memory bound. Cancel, deadline, loss and sink failure close only that child, report server completion unconfirmed, and never replay/reconnect it. Fresh export child cannot cancel another user's tab. Native error SQL/credential text discarded, private-parameter errors generic; process secrets use IPC, not argv/env. Original-host verified NodeTLS loopback bridge composes existing pinnedSSH transport. Native server TLS/SSH still unverified.

Validation evidence (runner logs `parallel-worker-b-<label>.log/json`):
- `db2-typecheck` initially failed3new interface mismatches (ForkOptions, cancel result, session state); corrected `db2-typecheck2` exit0,4.72s.
- `db2-unit` exit0,15/15 across policy/conversion, driver-double worker contract, REAL child blocking/cancel/stream backpressure, REAL genericTLS CA/hostname/no-preverify-forwarding. These are NOT nativeDb2 acceptance.
- `db2-build` initially failed test-only inferred return type after test addition; fixed explicit string|number annotation. `db2-build2` exit0,16.0s.
- `db2-regression` exit0,53pass/3nativeDb2 skips,9passedfiles+1skippedfile,1.97s. Includes16Db2 unit/protocol tests afteraggregate8MiB test, capabilities/persistence/SQL and managed/compatible policy. Two accidentally nonexistent filter names were not counted; corrected missing actual suites next.
- `db2-adapter-regression` exit0,38pass/6explicit fixture skips,3files,1.12s (`adapter-conformance`, `sql-table-query`, `connection-experience`).
- `db2-ui` failed31.93s because requiredschema had been placed under preferredschema. MovedDb2schema into an explicit mainform field. `db2-build-ui` exit0,15.93s. `db2-ui2` exit0,1/1nativeElectron test,3.05s wall/2.4stest: distinctengine/profile defaults, requiredschema, context/type/runtime limits, realforkedchild reports absentdriver withoutcredentialecho, emptyhistory, disabledRunScript. Screenshots `work/db2-form.png` and `work/db2-query.png` inspected; no clipping/overlap. Electron/userdata teardown done and coordinator notified slot release. Later empty-editor helper wording now agrees thatDb2 runsoneguardedstatement; no runtime logic changed.
- `db2-final-lint` exit0 for15ownedTypeScript/TSX files aftersource+testfixes; priorformatlimitedtoownedfiles only.

External native prerequisites remain exact blockers: official pinned12.1.2MacARM CLI archive download returnedHTTP403; actual CLI terms/checksum/library payload unavailable. IBM Community docs require LICENSE=accept plus privileged container; neither authorized/executed. Read-only manifest has amd64digest `sha256:e48ca934c29ab72d9e8b602929e6a52b64841cb7f8ef9426ee8cf5e1bc3e5fb3`, ppc64le ands390x, noARM64. NoDb2image pulled/container launched. Need approvedlicensed CLI/addon with OS-specific pins and authorized disposable LUWUTF8 endpoint/x86fixture, restrictedreader, nativeauth/TLS/SSH/types/cancel/deadline/memory/package/signing matrix. Three opt-in native fixture tests are prepared in `tests/db2.integration.test.ts`; no actual native tests passed. No cloud/provider activation, licensed acceptance, privileged containers, host/global config, repository commits/push/merge/deploy performed.

Final exact Db2 source build: `db2-final-build` exit0,31.97s (typecheck plus main/preload/renderer and separate Db2 child). This includes final owned formatting and the Db2 one-statement empty-editor hint. Frozen candidate/owned hashes and13 verified exact shared patch replays are recorded by `work/db2-checkpoint/manifest.json`. Later Cosmos/Firestore work must not modify this snapshot.
